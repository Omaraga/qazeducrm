# QazEduCRM Docker Compose Configuration
#
# Usage:
#   Local:  cp .env.local .env && docker compose up -d
#   Prod:   cp .env.production .env && docker compose up -d

services:
  # ============== NGINX ==============
  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./docker/nginx/ssl:/etc/nginx/ssl:ro
      - certbot_webroot:/var/www/certbot:ro
    depends_on:
      - php
      - evolution-api
    networks:
      - qazeducrm-network

  # ============== PHP-FPM ==============
  php:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    restart: unless-stopped
    volumes:
      - ./:/var/www/html
    environment:
      - DB_HOST=mysql
      - DB_NAME=${DB_NAME:-qazeducrm}
      - DB_USER=${DB_USER:-qazeducrm}
      - DB_PASS=${DB_PASS:-secret}
      - YII_DEBUG=${YII_DEBUG:-true}
      - YII_ENV=${YII_ENV:-dev}
      - EVOLUTION_API_URL=http://evolution-api:8080
      - EVOLUTION_API_KEY=${EVOLUTION_API_KEY:-your-api-key}
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - qazeducrm-network

  # ============== MySQL ==============
  mysql:
    image: mysql:8.0
    restart: unless-stopped
    command: --default-authentication-plugin=mysql_native_password
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD:-rootsecret}
      - MYSQL_DATABASE=${DB_NAME:-qazeducrm}
      - MYSQL_USER=${DB_USER:-qazeducrm}
      - MYSQL_PASSWORD=${DB_PASS:-secret}
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - qazeducrm-network

  # ============== EVOLUTION API ==============
  evolution-api:
    image: evoapicloud/evolution-api:v2.3.5
    restart: unless-stopped
    environment:
      - SERVER_URL=${EVOLUTION_SERVER_URL:-http://localhost/whatsapp}
      - AUTHENTICATION_TYPE=apikey
      - AUTHENTICATION_API_KEY=${EVOLUTION_API_KEY:-your-api-key}
      - DATABASE_ENABLED=true
      - DATABASE_PROVIDER=postgresql
      - DATABASE_CONNECTION_URI=postgresql://postgres:${POSTGRES_PASSWORD:-postgres}@postgres:5432/evolution
      - DATABASE_CONNECTION_CLIENT_NAME=evolution_api
      - CACHE_REDIS_ENABLED=true
      - CACHE_REDIS_URI=redis://redis:6379/0
      - CACHE_REDIS_PREFIX_KEY=evolution
      - CACHE_LOCAL_ENABLED=false
      - RABBITMQ_ENABLED=false
      - WEBSOCKET_ENABLED=false
      - DEL_INSTANCE=false
      - DEL_TEMP_INSTANCES=true
      - STORE_MESSAGES=true
      - STORE_MESSAGE_UP=true
      - STORE_CONTACTS=true
      - STORE_CHATS=true
      - LOG_LEVEL=INFO
      - LOG_COLOR=true
      - CORS_ORIGIN=*
      - CORS_METHODS=GET,POST,PUT,DELETE
      - CORS_CREDENTIALS=true
      - QRCODE_LIMIT=30
      # Глобальный Webhook для всех инстансов
      - WEBHOOK_GLOBAL_ENABLED=true
      - WEBHOOK_GLOBAL_URL=http://nginx/webhook/whatsapp
      - WEBHOOK_GLOBAL_WEBHOOK_BY_EVENTS=false
      - WEBHOOK_GLOBAL_HEADERS={"Host":"crm.qazaq.education"}
      - WEBHOOK_GLOBAL_EVENTS=APPLICATION_STARTUP,CONNECTION_UPDATE,QRCODE_UPDATED,MESSAGES_SET,MESSAGES_UPSERT,MESSAGES_UPDATE,MESSAGES_DELETE,SEND_MESSAGE
    volumes:
      - evolution_instances:/evolution/instances
      - evolution_store:/evolution/store
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - qazeducrm-network

  # ============== PostgreSQL (for Evolution API) ==============
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=evolution
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - qazeducrm-network

  # ============== Redis (for Evolution API) ==============
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - qazeducrm-network

volumes:
  mysql_data:
  postgres_data:
  redis_data:
  evolution_instances:
  evolution_store:
  certbot_webroot:

networks:
  qazeducrm-network:
    driver: bridge
