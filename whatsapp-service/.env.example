# Evolution API Configuration
# Copy this file to .env and fill in the values

# Server URL (where Evolution API is accessible)
# For local development:
SERVER_URL=http://localhost:8080
# For production, use your domain:
# SERVER_URL=https://whatsapp.yourdomain.com

# Port for Evolution API
EVOLUTION_PORT=8080

# API Key for authentication (CHANGE THIS!)
# Generate a secure key: openssl rand -hex 32
API_KEY=your-secret-api-key-change-me

# Webhook URL - where Evolution API sends events
# This should point to your CRM webhook endpoint
# Example: http://your-crm-domain/webhook/whatsapp
WEBHOOK_URL=http://host.docker.internal/qazeducrm/web/webhook/whatsapp

# For Windows with Docker Desktop, use:
# WEBHOOK_URL=http://host.docker.internal/qazeducrm/web/webhook/whatsapp

# For Linux with Docker, use host network IP:
# WEBHOOK_URL=http://172.17.0.1/qazeducrm/web/webhook/whatsapp
