# SSL certificates will be placed here
# Use certbot to generate certificates:
# docker run --rm -v $(pwd)/docker/nginx/ssl:/etc/letsencrypt certbot/certbot certonly --webroot -w /var/www/certbot -d yourdomain.com
